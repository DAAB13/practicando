# la principal razón de usar una tupla es cuidar la integridad de los datos
# Ejemplo: coordenadas GPS o los colores RGB

coordenadas_gps = (40.5635, -74.6567)
latitud = coordenadas_gps[0]
print(f"la latitud es: {latitud}")

# las tuplas no se puedes módificar
